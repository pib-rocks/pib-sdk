[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pib-sdk"
version = "0.3"
description = "sdk with helper classes and functions to control pib via rosbridge and solve kinematics"
readme = "README.md"
requires-python = ">=3.9,<3.12"
license = {file = "LICENSE"}

authors = [
  {name = "pib.rocks", email = "team@pib.rocks"}
]

dependencies = [
  "roboticstoolbox-python==1.1.0",
  "numpy==1.26.4",
  "scipy<1.12",
  "spatialmath-python",
  "roslibpy",
]

classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent"
]


[project.optional-dependencies]
dev = ["pytest", "ruff", "build", "twine"]

[project.urls]
Homepage = "https://github.com/pib-rocks/pib-sdk"

[tool.setuptools]               
package-dir = {"" = "src"}       # root of importable 
license-files = []  

[tool.setuptools.packages.find] 
where = ["src"]                  # scan “src/” for packages
