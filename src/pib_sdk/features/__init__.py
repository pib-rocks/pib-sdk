"""Optional, ready-to-use extras built on pib_sdk's core kinematics and control.

Each submodule here is a self-contained "cool feature" -- import it directly
rather than through this package, e.g.::

    from pib_sdk.features.drawing import image_to_sketch, DrawingSurface, sketch_to_trajectory

Submodules may need an extra dependency beyond pib_sdk's core install; each
documents its own requirement (e.g. ``pip install pib-sdk[drawing]``).
"""
